.DS_Store

# Large sequencing inputs and alignment outputs stay local.
fastq/*.fastq
fastq/*.fastq.gz
bam/*.bam
bam/*.bam.bai
bam/*.bam.fai

# Reference indexes are reproducible from the FASTA file.
fasta/*.amb
fasta/*.ann
fasta/*.bwt
fasta/*.fai
fasta/*.pac
fasta/*.sa